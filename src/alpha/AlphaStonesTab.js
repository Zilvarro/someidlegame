import {formatNumber} from '../utilities'

export default function AlphaStonesTab({state, popup, updateState, setTotalClicks}) {
    return (
        <div>{<>
            <h2>Starting Stones</h2>
            <p>(a.k.a. "Why am I even doing these?")</p>
            </>}
        </div>)
}